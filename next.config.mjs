/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['upload.wikimedia.org',"platform-lookaside.fbsbx.com","lh3.googleusercontent.com"]
    }
};

export default nextConfig;
